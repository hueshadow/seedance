# Seedance.free 网站产品需求文档（PRD）

> 版本：v1.0 | 日期：2026-02-13 | 状态：初稿

---

## 目录

1. [项目概述](#1-项目概述)
2. [站点架构（Sitemap）](#2-站点架构sitemap)
3. [各页面详细需求](#3-各页面详细需求)
4. [全局组件](#4-全局组件)
5. [技术要求](#5-技术要求)
6. [SEO 策略](#6-seo-策略)
7. [变现策略](#7-变现策略)
8. [分期交付计划](#8-分期交付计划)

---

## 1. 项目概述

### 1.1 项目背景

Seedance 是字节跳动 Seed 团队开发的 AI 视频生成模型，在角色一致性、物理真实感和原生音频同步方面处于行业领先地位。Seedance 2.0 于 2026 年 2 月发布，支持多模态参考输入（最多 12 个文件）、原生 2K 分辨率和多镜头叙事。

当前市场痛点：
- Seedance 的官方文档分散在即梦、豆包、小云雀等多个中文平台，缺乏统一的英文资源站
- 国际用户面临中国手机号注册壁垒，缺乏清晰的解决方案指引
- 关键词 "seedance" 搜索量 4,400/月（美国），"seedance 2.0" 为 Google Trends Breakout 查询，竞争度低（KD 16-39），存在巨大的 SEO 流量机会
- 高价值商业意图关键词如 "seedance api"（CPC $2.58）、"seedance price"（CPC $1.93）尚未被充分覆盖

### 1.2 项目目标

**短期目标（1个月内）：**
- 建立英文世界最权威的 Seedance 资源站
- 覆盖 Top 50 Seedance 相关关键词的 Google 首页排名
- 月访问量达到 10,000+ UV

**中期目标（3个月内）：**
- 月访问量达到 50,000+ UV
- AdSense 月收入 $500+
- 建立 Newsletter 订阅用户 2,000+

**长期目标（6个月内）：**
- 月访问量达到 200,000+ UV
- 付费内容/会员系统上线，月收入 $2,000+
- 成为 Seedance 生态的标准参考站点

### 1.3 目标用户画像

| 用户类型 | 描述 | 核心需求 | 对应页面 |
|---------|------|---------|---------|
| 内容创作者 | YouTube/TikTok 创作者，需要 AI 视频素材 | 快速上手、Prompt 技巧、免费使用方法 | `/guide`, `/prompts`, `/pricing` |
| 独立开发者 | 想将 Seedance 集成到产品中 | API 文档、代码示例、定价对比 | `/api`, `/pricing`, `/tools/calculator` |
| 电商从业者 | 需要批量生成产品视频 | Prompt 模板、批量工作流、成本计算 | `/prompts`, `/tools/prompt-builder`, `/tools/calculator` |
| AI 爱好者 | 关注 AI 视频生成领域动态 | 版本对比、竞品分析、最新资讯 | `/versions`, `/blog`, `/faq` |
| 海外用户 | 无法直接访问中国平台 | 注册指南、替代平台、API 接入 | `/register`, `/api`, `/pricing` |

### 1.4 技术栈决策

| 技术选型 | 方案 | 决策理由 |
|---------|------|---------|
| 框架 | Next.js 15 (App Router) | SSG/ISR 混合渲染、优秀的 SEO 支持、Vercel 原生部署 |
| 样式 | Tailwind CSS v4 | 原子化 CSS、零运行时开销、v4 支持 CSS-first 配置 |
| 内容管理 | MDX 文件驱动 | 无需数据库、Git 版本控制、支持 React 组件嵌入 |
| 部署 | Vercel | 全球 CDN、自动 CI/CD、Edge Functions 支持 |
| 分析 | Google Analytics 4 + Search Console | 免费、SEO 数据完整、与 AdSense 集成 |
| 广告 | Google AdSense | 最成熟的网站变现方案、自动广告优化 |
| 搜索 | Algolia DocSearch（或 Pagefind 自建） | Phase 1 用 Pagefind（零成本静态搜索），Phase 3 迁移 Algolia |
| 包管理 | pnpm | 更快的安装速度、严格的依赖管理 |
| 代码规范 | ESLint + Prettier + TypeScript strict | 类型安全、代码一致性 |

### 1.5 项目目录结构

```
seedance/
├── src/
│   ├── app/                          # Next.js App Router
│   │   ├── layout.tsx                # 根布局（Header + Footer）
│   │   ├── page.tsx                  # Homepage
│   │   ├── guide/
│   │   │   └── page.tsx              # Complete Guide
│   │   ├── versions/
│   │   │   └── page.tsx              # Version Comparison
│   │   ├── pricing/
│   │   │   └── page.tsx              # Pricing & Free Methods
│   │   ├── prompts/
│   │   │   └── page.tsx              # Prompt Library
│   │   ├── api/
│   │   │   └── page.tsx              # API Guide
│   │   ├── register/
│   │   │   └── page.tsx              # Registration Guide
│   │   ├── tools/
│   │   │   ├── prompt-builder/
│   │   │   │   └── page.tsx          # Prompt Builder Tool
│   │   │   ├── compare/
│   │   │   │   └── page.tsx          # Model Comparison Tool
│   │   │   └── calculator/
│   │   │       └── page.tsx          # Credit Calculator
│   │   ├── blog/
│   │   │   ├── page.tsx              # Blog 列表页
│   │   │   └── [slug]/
│   │   │       └── page.tsx          # Blog 文章页
│   │   ├── faq/
│   │   │   └── page.tsx              # FAQ
│   │   └── about/
│   │       └── page.tsx              # About
│   ├── components/
│   │   ├── layout/
│   │   │   ├── Header.tsx            # 全局导航栏
│   │   │   ├── Footer.tsx            # 全局页脚
│   │   │   ├── Sidebar.tsx           # 侧边栏（TOC + 广告位）
│   │   │   └── Breadcrumbs.tsx       # 面包屑导航
│   │   ├── ui/
│   │   │   ├── Button.tsx
│   │   │   ├── Card.tsx
│   │   │   ├── Badge.tsx
│   │   │   ├── Tabs.tsx
│   │   │   ├── Table.tsx
│   │   │   ├── CopyButton.tsx        # 一键复制按钮
│   │   │   ├── CodeBlock.tsx         # 代码块 + 语法高亮
│   │   │   ├── SearchBar.tsx         # 搜索组件
│   │   │   └── ThemeToggle.tsx       # 暗色模式切换
│   │   ├── home/
│   │   │   ├── Hero.tsx              # 首页 Hero 区域
│   │   │   ├── QuickNavCards.tsx     # 快速导航卡片
│   │   │   ├── VersionStatus.tsx     # 版本状态指示器
│   │   │   ├── ComparisonSummary.tsx # 竞品对比摘要
│   │   │   └── LatestPosts.tsx       # 最新博客文章
│   │   ├── guide/
│   │   │   ├── TableOfContents.tsx   # 左侧 TOC 导航
│   │   │   └── SectionCTA.tsx        # 段落末尾 CTA
│   │   ├── versions/
│   │   │   ├── ComparisonTable.tsx   # 交互式对比表
│   │   │   ├── VersionCard.tsx       # 版本详情卡片
│   │   │   └── DecisionFlow.tsx      # 决策流程图
│   │   ├── pricing/
│   │   │   ├── PricingTable.tsx      # 价格对比表
│   │   │   ├── FreeMethodsSection.tsx # 免费方法专区
│   │   │   └── CreditCalculator.tsx  # 信用点计算器（内嵌版）
│   │   ├── prompts/
│   │   │   ├── PromptCard.tsx        # Prompt 卡片
│   │   │   ├── PromptFilter.tsx      # 搜索和筛选
│   │   │   └── CategoryNav.tsx       # 分类导航
│   │   ├── tools/
│   │   │   ├── PromptBuilderForm.tsx # Prompt 构建器表单
│   │   │   ├── PromptPreview.tsx     # 实时预览
│   │   │   ├── ReferenceTagHelper.tsx # @Reference 标签辅助
│   │   │   ├── ModelCompare.tsx      # 模型对比工具
│   │   │   └── Calculator.tsx        # 费用计算器
│   │   ├── blog/
│   │   │   ├── PostCard.tsx          # 文章卡片
│   │   │   ├── TagList.tsx           # 标签列表
│   │   │   └── RelatedPosts.tsx      # 相关文章推荐
│   │   ├── api-guide/
│   │   │   ├── PlatformTabs.tsx      # 平台 Tab 切换
│   │   │   ├── ParamTable.tsx        # 参数说明表
│   │   │   └── ErrorGuide.tsx        # 错误排查指南
│   │   ├── register/
│   │   │   ├── StepByStep.tsx        # 图文教程步骤
│   │   │   └── PlatformComparison.tsx # 平台注册对比
│   │   ├── faq/
│   │   │   └── FAQAccordion.tsx      # FAQ 手风琴组件
│   │   ├── ads/
│   │   │   ├── AdSlot.tsx            # 广告位组件
│   │   │   └── AdSenseScript.tsx     # AdSense 脚本加载
│   │   └── seo/
│   │       ├── JsonLd.tsx            # 结构化数据组件
│   │       └── MetaTags.tsx          # Meta 标签组件
│   ├── content/
│   │   ├── blog/                     # MDX 博客文章
│   │   │   ├── seedance-vs-sora.mdx
│   │   │   ├── seedance-vs-veo3.mdx
│   │   │   └── ...
│   │   ├── prompts/                  # Prompt 数据
│   │   │   └── prompts.json
│   │   └── faq/                      # FAQ 数据
│   │       └── faq.json
│   ├── lib/
│   │   ├── mdx.ts                    # MDX 处理工具
│   │   ├── search.ts                 # 搜索索引构建
│   │   ├── seo.ts                    # SEO 工具函数
│   │   └── constants.ts              # 全局常量
│   ├── data/
│   │   ├── versions.ts               # 版本数据
│   │   ├── pricing.ts                # 定价数据
│   │   ├── platforms.ts              # 平台数据
│   │   └── navigation.ts             # 导航数据
│   └── types/
│       ├── blog.ts                   # 博客类型定义
│       ├── prompt.ts                 # Prompt 类型定义
│       └── version.ts                # 版本类型定义
├── public/
│   ├── images/
│   │   ├── og/                       # Open Graph 图片
│   │   ├── guide/                    # 指南配图
│   │   ├── register/                 # 注册教程截图
│   │   └── versions/                 # 版本对比图
│   ├── robots.txt
│   └── sitemap.xml                   # 自动生成
├── next.config.ts
├── tailwind.config.ts
├── tsconfig.json
├── package.json
└── .env.local                        # 环境变量（GA_ID, ADSENSE_ID 等）
```

---

## 2. 站点架构（Sitemap）

### 2.1 URL 结构

```
seedance.free/
├── /                                  # Homepage（首页）
├── /guide                             # Complete Guide（完整指南 - 主力 SEO 页）
├── /versions                          # Version Comparison（版本对比）
├── /pricing                           # Pricing & Free Methods（定价与免费方法）
├── /prompts                           # Prompt Library（Prompt 库）
├── /api                               # API Integration Guide（API 集成指南）
├── /register                          # Registration Guide（注册指南）
├── /tools/
│   ├── /tools/prompt-builder          # Prompt Builder Tool（Prompt 构建器）
│   ├── /tools/compare                 # Side-by-side Model Comparison（模型对比工具）
│   └── /tools/calculator              # Credit/Cost Calculator（费用计算器）
├── /blog                              # Blog Index（博客列表）
│   ├── /blog/seedance-vs-sora         # 对比文章
│   ├── /blog/seedance-vs-veo3         # 对比文章
│   ├── /blog/best-seedance-prompts-ecommerce  # Prompt 教程
│   ├── /blog/seedance-outside-china   # 海外访问指南
│   ├── /blog/seedance-api-tutorial    # API 教程
│   └── /blog/[slug]                   # 动态博客文章
├── /faq                               # FAQ（常见问题 - 结构化数据）
└── /about                             # About（关于）
```

### 2.2 页面层级与内部链接策略

```
                          ┌─────────┐
                          │Homepage │
                          │   /     │
                          └────┬────┘
                               │
          ┌────────────────────┼────────────────────┐
          │                    │                     │
    ┌─────┴─────┐      ┌──────┴──────┐      ┌──────┴──────┐
    │  核心内容   │      │   工具页面   │      │  博客/FAQ   │
    │  Pillar    │      │   Tools     │      │  Content    │
    └─────┬─────┘      └──────┬──────┘      └──────┬──────┘
          │                    │                     │
    ┌─────┼─────┐        ┌────┼────┐          ┌────┼────┐
    │     │     │        │    │    │          │    │    │
  /guide  │  /register   │    │    │        /blog │  /faq
/versions │  /api     /tools/ │  /tools/  /blog/  │
/pricing  │          prompt   │  calc     [slug]  │
/prompts  │          builder  │                   │
          │                /tools/                 │
          │                compare                 │
          │                                        │
          └──────── 交叉内链 ──────────────────────┘
```

内部链接规则：
- 每个 Pillar 页面（guide, versions, pricing, prompts, api, register）互相链接
- 每篇博客文章至少链接到 2 个 Pillar 页面
- 工具页面链接回对应的内容页面（如 prompt-builder → /prompts, calculator → /pricing）
- FAQ 每个答案链接到对应的详情页面
- Homepage 链接到所有一级页面

### 2.3 页面渲染策略

| 页面 | 渲染方式 | 理由 |
|------|---------|------|
| `/` | SSG | 内容相对固定，构建时生成 |
| `/guide` | SSG | 长内容页，SEO 关键页面 |
| `/versions` | SSG | 数据更新频率低 |
| `/pricing` | ISR (revalidate: 86400) | 价格可能变动，每日重新验证 |
| `/prompts` | SSG + CSR | 列表 SSG，筛选/搜索 CSR |
| `/api` | SSG | 文档内容，构建时生成 |
| `/register` | SSG | 教程内容固定 |
| `/tools/*` | CSR | 纯交互工具，无 SEO 内容需求（但保留 SSG shell） |
| `/blog` | SSG | MDX 构建时编译 |
| `/blog/[slug]` | SSG (generateStaticParams) | 每篇文章构建时生成 |
| `/faq` | SSG | 结构化数据需要服务端渲染 |
| `/about` | SSG | 静态内容 |

---

## 3. 各页面详细需求

### 3.1 Homepage（首页 `/`）

**页面目标：** 作为全站入口，快速传达 Seedance 的价值主张，引导用户进入对应的功能页面。

**目标关键词：**
- 主关键词：`seedance`（4,400/月）、`seedance ai`（1,300/月）
- 长尾关键词：`seedance official website`、`seedance ai video generator`、`what is seedance`

**Schema Markup：** `WebSite` + `Organization` + `SearchAction`

```json
{
  "@context": "https://schema.org",
  "@type": "WebSite",
  "name": "Seedance.free",
  "url": "https://seedance.free",
  "description": "The complete Seedance resource for creators and developers",
  "potentialAction": {
    "@type": "SearchAction",
    "target": "https://seedance.free/search?q={search_term_string}",
    "query-input": "required name=search_term_string"
  }
}
```

**页面布局：**

```
┌─────────────────────────────────────────────────────┐
│                    Header                            │
├─────────────────────────────────────────────────────┤
│                                                      │
│  ┌───────────────────────────────────────────────┐  │
│  │              Hero Section                      │  │
│  │                                                │  │
│  │  "The Complete Seedance Resource               │  │
│  │   for Creators & Developers"                   │  │
│  │                                                │  │
│  │  副标题: Everything you need to master          │  │
│  │  ByteDance's AI video generation model         │  │
│  │                                                │  │
│  │  [Start Using Seedance →] [Read the Guide]     │  │
│  │                                                │  │
│  │  ┌─────────────────────────────────────────┐   │  │
│  │  │  Version Status Badge:                  │   │  │
│  │  │  🟢 Seedance 2.0 - Available on Jimeng  │   │  │
│  │  │  🟡 Seedance 2.0 API - Just Launched    │   │  │
│  │  └─────────────────────────────────────────┘   │  │
│  └───────────────────────────────────────────────┘  │
│                                                      │
│  ┌───────────────────────────────────────────────┐  │
│  │           Quick Navigation Cards               │  │
│  │                                                │  │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐         │  │
│  │  │Guide │ │Tools │ │ API  │ │Regis-│         │  │
│  │  │      │ │      │ │      │ │ ter  │         │  │
│  │  │完整指 │ │Prompt│ │集成指 │ │注册指 │         │  │
│  │  │南    │ │Builder│ │南    │ │南    │         │  │
│  │  └──────┘ └──────┘ └──────┘ └──────┘         │  │
│  └───────────────────────────────────────────────┘  │
│                                                      │
│  ┌───────────────────────────────────────────────┐  │
│  │        Competitor Comparison Summary            │  │
│  │                                                │  │
│  │  Seedance 2.0 vs Sora 2 vs Veo 3.1 vs Kling  │  │
│  │  ┌─────────────────────────────────────────┐   │  │
│  │  │  简化对比表（4列 x 6行）                  │   │  │
│  │  │  Resolution / Duration / Audio / Price   │   │  │
│  │  │  / Free Tier / API                       │   │  │
│  │  └─────────────────────────────────────────┘   │  │
│  │  [View Full Comparison →]                      │  │
│  └───────────────────────────────────────────────┘  │
│                                                      │
│  ┌───────────────────────────────────────────────┐  │
│  │           Latest Blog Posts                    │  │
│  │                                                │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐      │  │
│  │  │ Post 1   │ │ Post 2   │ │ Post 3   │      │  │
│  │  │ 缩略图    │ │ 缩略图    │ │ 缩略图    │      │  │
│  │  │ 标题      │ │ 标题      │ │ 标题      │      │  │
│  │  │ 摘要      │ │ 摘要      │ │ 摘要      │      │  │
│  │  └──────────┘ └──────────┘ └──────────┘      │  │
│  │  [View All Posts →]                            │  │
│  └───────────────────────────────────────────────┘  │
│                                                      │
│  ┌───────────────────────────────────────────────┐  │
│  │              CTA Section                       │  │
│  │  "Ready to create amazing AI videos?"          │  │
│  │  [Start Using Seedance →]                      │  │
│  └───────────────────────────────────────────────┘  │
│                                                      │
├─────────────────────────────────────────────────────┤
│                    Footer                            │
└─────────────────────────────────────────────────────┘
```

**组件实现细节：**

**Hero 组件 (`Hero.tsx`)：**
- 背景：深色渐变（`bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900`），可选加入微妙的网格动画
- 标题：`<h1>` 标签，字体 Inter Bold，48px desktop / 32px mobile
- 副标题：`<p>` 标签，text-lg，text-slate-300
- CTA 按钮组：主按钮（实心，链接到 `/guide`）+ 次按钮（描边，链接到 `/guide`）
- 版本状态徽章：实时显示最新版本状态，使用 `<Badge>` 组件，绿色=可用，黄色=新发布，灰色=即将推出

**快速导航卡片 (`QuickNavCards.tsx`)：**
```typescript
interface NavCard {
  title: string;        // "Complete Guide"
  description: string;  // "Learn everything about Seedance"
  icon: ReactNode;      // Lucide icon
  href: string;         // "/guide"
  badge?: string;       // "Popular" | "New"
}
```
- 4 列网格布局（desktop），2 列（tablet），1 列（mobile）
- 每张卡片 hover 效果：轻微上移 + 阴影加深
- 卡片内容：图标 + 标题 + 一句话描述 + 箭头指示

**竞品对比摘要 (`ComparisonSummary.tsx`)：**
- 简化版对比表，仅展示 6 个核心维度
- 使用 `<Table>` 组件，支持横向滚动（mobile）
- Seedance 列高亮显示（背景色区分）
- 底部 CTA 链接到 `/versions` 完整对比页

**最新博客文章 (`LatestPosts.tsx`)：**
- 3 列卡片布局，展示最新 3 篇文章
- 每张卡片：缩略图（16:9）+ 标题 + 发布日期 + 摘要（2行截断）
- 数据来源：构建时从 `/content/blog/` 读取 MDX frontmatter

**CTA 位置：**
1. Hero 区域底部（主 CTA）
2. 竞品对比表下方（"View Full Comparison"）
3. 博客列表下方（"View All Posts"）
4. 页面底部独立 CTA 区域（"Ready to create amazing AI videos?"）

### 3.2 Complete Guide（完整指南 `/guide`）

**页面目标：** 全站最重要的 SEO 页面，覆盖 Seedance 从入门到精通的所有内容。基于已有的 `how-to-use-seedance-complete-guide.md` 内容扩展。

**目标关键词：**
- 主关键词：`how to use seedance`（30/月，Breakout 趋势）、`seedance guide`
- 长尾关键词：`how to use seedance ai`、`how to use seedance for free`、`how to use seedance 1.0`、`seedance tutorial`、`seedance prompt guide`

**Schema Markup：** `HowTo` + `Article`

```json
{
  "@context": "https://schema.org",
  "@type": "HowTo",
  "name": "How to Use Seedance: The Complete Guide",
  "description": "Learn everything about ByteDance's Seedance AI video generation model",
  "step": [
    {
      "@type": "HowToStep",
      "name": "Understand Seedance Versions",
      "text": "Compare Seedance 1.0, 1.5, and 2.0..."
    },
    {
      "@type": "HowToStep",
      "name": "Choose an Access Method",
      "text": "Select from Jimeng, Dreamina, third-party platforms, or API..."
    }
  ]
}
```

**页面布局：**

```
┌──────────────────────────────────────────────────────────┐
│ Breadcrumbs: Home > Guide                                │
├──────────┬───────────────────────────────────┬───────────┤
│          │                                   │           │
│  TOC     │     Main Content Area             │  Sidebar  │
│ (sticky) │                                   │           │
│          │  ┌─────────────────────────────┐   │  Ad Slot  │
│ - What   │  │ H1: How to Use Seedance:    │   │           │
│ - Vers.  │  │ The Complete Guide (2026)    │   │  Related  │
│ - Access │  │                             │   │  Articles │
│ - Phone  │  │ Last updated: Feb 13, 2026  │   │           │
│ - Prompt │  │ Reading time: 15 min        │   │  Newsletter│
│ - API    │  └─────────────────────────────┘   │  Signup   │
│ - FAQ    │                                   │           │
│          │  Section 1: What Is Seedance?     │           │
│          │  [内容] + [CTA → /versions]       │           │
│          │                                   │           │
│          │  Section 2: Versions Explained    │           │
│          │  [版本对比表] + [CTA → /versions] │           │
│          │                                   │           │
│          │  ── Ad Slot (文章中间) ──          │           │
│          │                                   │           │
│          │  Section 3: Access Methods        │           │
│          │  [各平台介绍] + [CTA → /pricing]  │           │
│          │                                   │           │
│          │  Section 4: Phone Registration    │           │
│          │  [注册教程] + [CTA → /register]   │           │
│          │                                   │           │
│          │  Section 5: Prompt Guide          │           │
│          │  [Prompt技巧] + [CTA → /prompts]  │           │
│          │                                   │           │
│          │  ── Ad Slot (文章中间) ──          │           │
│          │                                   │           │
│          │  Section 6: API Integration       │           │
│          │  [API概览] + [CTA → /api]         │           │
│          │                                   │           │
│          │  Section 7: FAQ                   │           │
│          │  [常见问题] + [CTA → /faq]        │           │
│          │                                   │           │
│          │  ── Ad Slot (文章底部) ──          │           │
│          │                                   │           │
├──────────┴───────────────────────────────────┴───────────┤
│ Footer                                                    │
└──────────────────────────────────────────────────────────┘
```

**TOC 组件 (`TableOfContents.tsx`)：**
- 左侧固定定位（`sticky top-24`），桌面端可见，移动端折叠为顶部下拉
- 自动从页面 `<h2>` / `<h3>` 标签生成目录
- 当前阅读位置高亮（Intersection Observer API）
- 点击平滑滚动到对应章节
- 实现方式：使用 `useEffect` + `IntersectionObserver` 监听各 section 的可见性

**段落末尾 CTA (`SectionCTA.tsx`)：**
```typescript
interface SectionCTAProps {
  text: string;       // "Learn more about Seedance versions"
  href: string;       // "/versions"
  variant: 'primary' | 'secondary';
}
```
- 每个主要章节末尾放置一个 CTA 链接到对应的详情页
- 样式：带箭头的文字链接或轻量级按钮，不要过于突兀

**内容分段与对应详情页映射：**

| 章节 | 内容概要 | CTA 目标 |
|------|---------|---------|
| What Is Seedance? | 简介、核心能力 | — |
| Versions Explained | 版本对比表（简化版） | `/versions` |
| Access Methods | 5种访问方式概览 | `/pricing` |
| Phone Registration | 注册流程概要 | `/register` |
| Prompt Guide | Prompt 基础技巧 | `/prompts` + `/tools/prompt-builder` |
| API Integration | API 平台概览 | `/api` |
| FAQ | 高频问题 | `/faq` |

**广告位：**
- 文章中间（Section 3 和 Section 4 之间）：横幅广告
- 文章中间（Section 5 和 Section 6 之间）：横幅广告
- 文章底部：横幅广告
- 右侧 Sidebar：固定广告位


### 3.3 Version Comparison（版本对比 `/versions`）

**页面目标：** 提供 Seedance 所有版本的详细对比，帮助用户选择最适合的版本。

**目标关键词：**
- 主关键词：`seedance 1.0`（2,400/月）、`seedance 2.0`（30/月，Breakout）、`seedance 1.5`
- 长尾关键词：`seedance 1.0 pro`（480/月）、`seedance 1.0 lite`（110/月）、`seedance 1.0 mini vs pro`、`seedance pro`（390/月）

**Schema Markup：** `Table` + `Product`（每个版本作为一个 Product）

**页面内容结构：**

1. **页面标题区域**
   - H1: "Seedance Versions Compared: 1.0 → 1.5 → 2.0"
   - 副标题: "Find the right Seedance model for your needs"

2. **交互式对比表 (`ComparisonTable.tsx`)**

   数据结构：
   ```typescript
   interface VersionData {
     id: string;                    // "seedance-2.0"
     name: string;                  // "Seedance 2.0"
     releaseDate: string;           // "2026-02"
     resolution: string;            // "Up to 2K"
     maxDuration: string;           // "4-15s"
     speed: 'fast' | 'standard';
     hasAudio: boolean;
     hasLipSync: boolean;
     maxReferences: number;         // 12
     aspectRatios: string[];        // ["16:9", "9:16", ...]
     platforms: string[];           // ["Jimeng", "Volcano Engine API"]
     priceRange: string;            // "¥69/mo (~$9.60)"
     bestFor: string;               // "Full multimodal control"
     highlights: string[];          // ["Native 2K", "Multi-shot", ...]
   }
   ```

   表格列：Version | Resolution | Duration | Speed | Audio | References | Platforms | Price
   
   交互功能：
   - 列排序（点击表头）
   - 行高亮（hover）
   - 版本筛选（勾选要对比的版本）
   - 移动端横向滚动，固定第一列（版本名称）

3. **版本详情卡片 (`VersionCard.tsx`)**
   - 每个版本一张展开式卡片
   - 包含：版本名称、发布日期、核心参数、支持平台、价格、适用场景
   - 卡片内 CTA："Use on Jimeng →" / "Try via API →"

4. **决策流程图 (`DecisionFlow.tsx`)**
   - "Which Seedance version is right for you?"
   - 交互式决策树：
     ```
     需要音频吗？
     ├── 是 → 需要 2K 分辨率吗？
     │        ├── 是 → Seedance 2.0（推荐 Jimeng）
     │        └── 否 → Seedance 1.5 Pro（推荐 Dreamina）
     └── 否 → 需要高质量吗？
              ├── 是 → Seedance 1.0 Pro（推荐 fal.ai / Replicate）
              └── 否 → 需要快速生成吗？
                       ├── 是 → Seedance 1.0 Lite / Fast
                       └── 否 → Seedance 1.0 Mini
     ```
   - 纯前端实现，使用状态机管理流程
   - 每个终端节点显示推荐版本 + 推荐平台 + CTA 链接

5. **竞品对比区域**
   - Seedance 2.0 vs Sora 2 vs Veo 3.1 vs Kling 2.0
   - 使用已有的对比数据（来自 guide 文档）
   - CTA: "Read detailed comparison →" 链接到对应博客文章

**CTA 位置：**
- 每个版本卡片内（链接到对应平台）
- 决策流程图结果处（链接到对应平台/页面）
- 页面底部（"Start with Seedance 2.0 →" 链接到 `/register`）

### 3.4 Pricing & Free Methods（定价与免费方法 `/pricing`）

**页面目标：** 全面展示 Seedance 在各平台的定价，突出免费使用方法，提供成本计算工具。

**目标关键词：**
- 主关键词：`seedance price`（110/月，CPC $1.93）、`seedance pricing`（40/月）、`seedance free`（170/月）
- 长尾关键词：`seedance 1.0 free`（480/月）、`seedance ai free`（210/月）、`is seedance free`、`seedance pro free`、`use seedance for free`、`seedance free trial`

**Schema Markup：** `Product` + `Offer`

```json
{
  "@context": "https://schema.org",
  "@type": "Product",
  "name": "Seedance 2.0",
  "description": "AI video generation model by ByteDance",
  "offers": [
    {
      "@type": "Offer",
      "name": "Jimeng Free Tier",
      "price": "0",
      "priceCurrency": "USD",
      "description": "~260 free credits for new users"
    },
    {
      "@type": "Offer",
      "name": "Jimeng Basic Plan",
      "price": "9.60",
      "priceCurrency": "USD",
      "priceSpecification": {
        "@type": "UnitPriceSpecification",
        "billingDuration": "P1M"
      }
    }
  ]
}
```

**页面内容结构：**

1. **"Free Methods" 专区（页面顶部突出展示）**
   - 绿色高亮背景区域
   - 标题: "How to Use Seedance for Free"
   - 3 列卡片：
     - Jimeng: ~260 free credits for new users
     - Xiaoyunque: 1,200 bonus + 120 daily credits
     - Doubao: 10 free generations/day
   - 每张卡片包含：平台名、免费额度、限制条件、注册链接
   - 注意标注：需要中国手机号的平台加 warning badge

2. **各平台价格对比表 (`PricingTable.tsx`)**

   数据结构：
   ```typescript
   interface PlatformPricing {
     platform: string;           // "Jimeng (China)"
     seedanceVersions: string[]; // ["1.0", "1.5", "2.0"]
     freeTier: string;           // "~260 credits"
     plans: {
       name: string;             // "Basic"
       price: number;            // 9.60
       currency: string;         // "USD"
       period: 'monthly' | 'yearly' | 'per-use';
       credits: number;          // 500
       features: string[];
     }[];
     requiresChinesePhone: boolean;
     apiAvailable: boolean;
     website: string;
   }
   ```

   平台列表：
   | 平台 | Seedance 版本 | 免费额度 | 起步价 | 需要中国手机号 |
   |------|-------------|---------|-------|-------------|
   | Jimeng (China) | 1.0/1.5/2.0 | ~260 credits | ¥69/mo (~$9.60) | Yes |
   | Dreamina (International) | 1.0/1.5 | 60+ daily credits | $18/mo | No |
   | Xiaoyunque | 2.0 | 1,200 + 120/day | ¥39/mo (~$5.40) | Yes |
   | Doubao | 2.0 | 10 gens/day | Free (waitlist) | Yes |
   | fal.ai | 1.0 Pro/Lite | Pay-per-use | ~$0.05/gen | No |
   | Replicate | 1.0 Pro | Pay-per-use | ~$0.05/gen | No |
   | Pollo AI | 2.0 (wrapper) | Limited free | Credits-based | No |
   | HuggingFace | 1.0 Pro (demo) | Free (limited) | Free | No |

3. **竞品价格对比**
   - Seedance vs Sora 2 ($200/mo) vs Veo 3.1 ($19.99/mo) vs Kling ($9.99/mo)
   - 突出 Seedance 的价格优势
   - 按"每秒视频成本"标准化对比

4. **信用点计算器（内嵌版 `CreditCalculator.tsx`）**
   - 输入字段：
     - 视频数量（slider: 1-100）
     - 每个视频时长（dropdown: 4s/5s/8s/10s/15s）
     - 分辨率（dropdown: 480p/720p/1080p/2K）
     - 平台选择（dropdown）
   - 输出：
     - 预估总费用
     - 推荐套餐
     - 与竞品费用对比
   - CTA: "Try the full calculator →" 链接到 `/tools/calculator`

**CTA 位置：**
- Free Methods 区域每张卡片（链接到对应平台或 `/register`）
- 价格表每行（链接到对应平台）
- 计算器结果处（链接到推荐平台）
- 页面底部（"Start for Free →"）

### 3.5 Prompt Library（Prompt 库 `/prompts`）

**页面目标：** 提供可直接使用的 Seedance prompt 模板库，支持分类浏览、搜索和一键复制。

**目标关键词：**
- 主关键词：`seedance prompt`（40/月）、`seedance prompt examples`
- 长尾关键词：`seedance prompt guide`、`seedance 1.0 prompt guide`、`seedance pro prompt guide`、`seedance 1.0 prompt examples`

**Schema Markup：** `ItemList` + `CreativeWork`

**页面内容结构：**

1. **页面标题 + 搜索栏**
   - H1: "Seedance Prompt Library"
   - 全文搜索框（搜索 prompt 标题、描述、标签）
   - 筛选器：分类 + 版本兼容性 + 难度

2. **分类导航 (`CategoryNav.tsx`)**
   - 横向标签栏（可滚动）
   - 分类列表：
     - All（全部）
     - E-commerce（电商产品视频）
     - Short Film（短片/剧情）
     - Music Video（音乐视频/可视化）
     - Social Media（社交媒体内容）
     - Product Demo（产品演示）
     - Animation（动画/特效）
     - Nature（自然/风景）
     - Portrait（人像/角色）

3. **Prompt 卡片网格 (`PromptCard.tsx`)**

   数据结构：
   ```typescript
   interface Prompt {
     id: string;
     title: string;              // "Cinematic Product Orbit"
     description: string;        // "360-degree product showcase..."
     prompt: string;             // 完整 prompt 文本
     category: PromptCategory;
     tags: string[];             // ["e-commerce", "product", "360"]
     compatibleVersions: string[]; // ["1.0 Pro", "1.5 Pro", "2.0"]
     difficulty: 'beginner' | 'intermediate' | 'advanced';
     usesReferences: boolean;    // 是否使用 @Reference
     referenceTypes?: string[];  // ["@Image1", "@Video1"]
     isPremium: boolean;         // Phase 2: 付费 prompt
     copyCount?: number;         // 复制次数统计
     createdAt: string;
   }
   ```

   卡片布局：
   - 3 列网格（desktop），2 列（tablet），1 列（mobile）
   - 每张卡片包含：
     - 标题
     - 分类 Badge
     - 预览描述（2行截断）
     - 兼容版本标签
     - 难度指示器（绿/黄/红点）
     - "Copy Prompt" 按钮（点击复制完整 prompt 到剪贴板）
     - 展开/折叠查看完整 prompt
   - 付费 prompt 显示锁定图标 + "Unlock" 按钮（Phase 2）

4. **搜索和筛选 (`PromptFilter.tsx`)**
   - 实时搜索（debounce 300ms）
   - 多选筛选器：分类、版本、难度、是否使用 Reference
   - URL 参数同步（`/prompts?category=ecommerce&version=2.0`）
   - 结果计数显示

5. **Prompt 提交入口（Phase 2）**
   - "Submit Your Prompt" CTA 按钮
   - 链接到提交表单（需要用户登录）

**初始 Prompt 数据（至少 30 个）：**

| 分类 | 数量 | 示例 |
|------|------|------|
| E-commerce | 6 | 产品 360 旋转、开箱动画、使用场景展示 |
| Short Film | 5 | 黑色电影场景、追逐戏、对话场景 |
| Music Video | 4 | 抽象流体、节奏同步、舞蹈编排 |
| Social Media | 5 | 说话头像、产品展示、Before/After |
| Product Demo | 4 | 软件界面、App 演示、功能展示 |
| Animation | 3 | 角色动画、特效转场、Logo 动画 |
| Nature | 2 | 风景延时、野生动物 |
| Portrait | 3 | 角色一致性、表情变化、服装展示 |

**CTA 位置：**
- 搜索栏旁（"Build Custom Prompt →" 链接到 `/tools/prompt-builder`）
- 页面底部（"Can't find what you need? Build your own →"）

### 3.6 Prompt Builder Tool（Prompt 构建器 `/tools/prompt-builder`）

**页面目标：** 提供交互式 Prompt 构建工具，帮助用户通过表单选择生成高质量 Seedance prompt。

**目标关键词：**
- `seedance prompt builder`、`seedance prompt generator`、`seedance text to video`（30/月）

**Schema Markup：** `WebApplication`

```json
{
  "@context": "https://schema.org",
  "@type": "WebApplication",
  "name": "Seedance Prompt Builder",
  "description": "Build optimized prompts for Seedance AI video generation",
  "applicationCategory": "AI Tool",
  "operatingSystem": "Web Browser",
  "offers": {
    "@type": "Offer",
    "price": "0",
    "priceCurrency": "USD"
  }
}
```

**工具功能设计：**

**表单区域 (`PromptBuilderForm.tsx`)：**

```typescript
interface PromptBuilderState {
  // 基础设置
  sceneType: 'product' | 'character' | 'landscape' | 'abstract' | 'social' | 'custom';
  
  // 主体描述
  subject: string;              // 自由文本输入
  subjectDetails: string;       // 补充描述
  
  // 动作/运动
  action: string;               // 预设选项 + 自定义
  actionPresets: string[];      // ["walks", "runs", "dances", "speaks", "orbits", ...]
  
  // 镜头设置
  cameraMovement: string;       // 预设选项
  cameraPresets: string[];      // ["static", "slow pan left", "dolly in", "tracking shot", "aerial", "orbit 360", ...]
  
  // 光线
  lighting: string;             // 预设选项
  lightingPresets: string[];    // ["natural daylight", "golden hour", "studio", "neon", "candlelit", ...]
  
  // 风格
  style: string;                // 预设选项
  stylePresets: string[];       // ["cinematic", "documentary", "anime", "minimalist", "vintage", ...]
  
  // 技术参数
  duration: number;             // 4-15 秒
  aspectRatio: string;          // "16:9" | "9:16" | "1:1" | ...
  
  // @Reference 设置
  references: {
    type: 'image' | 'video' | 'audio';
    tag: string;                // "@Image1"
    role: string;               // "main character" | "style reference" | "motion reference" | ...
  }[];
}
```

表单布局：
- Step 1: 选择场景类型（大图标卡片选择）
- Step 2: 描述主体（文本输入 + AI 建议）
- Step 3: 选择动作（预设按钮组 + 自定义输入）
- Step 4: 镜头和光线（下拉选择）
- Step 5: 风格和参数（下拉选择 + slider）
- Step 6: 添加 Reference（可选，动态添加）

**实时预览 (`PromptPreview.tsx`)：**
- 右侧固定面板（desktop），底部固定面板（mobile）
- 实时拼接用户选择生成 prompt 文本
- 语法高亮：@Reference 标签用特殊颜色标注
- "Copy to Clipboard" 按钮
- "Reset" 按钮
- 字符计数

**@Reference 标签辅助 (`ReferenceTagHelper.tsx`)：**
- 动态添加 Reference 条目
- 选择类型（Image/Video/Audio）→ 自动生成标签（@Image1, @Video1, @Audio1）
- 选择角色（主角、风格参考、动作参考、背景音乐等）
- 自动在 prompt 中插入对应的 @mention 语法
- 最多支持：9 Image + 3 Video + 3 Audio（与 Seedance 2.0 限制一致）

**纯前端实现：** 无需 API 调用，所有逻辑在客户端完成。使用 React state 管理表单状态，`useCallback` 优化 prompt 拼接性能。

### 3.7 Credit Calculator（费用计算器 `/tools/calculator`）

**页面目标：** 帮助用户精确计算 Seedance 使用成本，对比不同平台和竞品的费用。

**目标关键词：**
- `seedance pricing`（40/月）、`seedance price`（110/月）、`seedance api pricing`

**Schema Markup：** `WebApplication`

**计算器功能设计 (`Calculator.tsx`)：**

输入区域：
```typescript
interface CalculatorInput {
  videoCount: number;           // 1-500
  durationPerVideo: number;     // 4-15 秒
  resolution: '480p' | '720p' | '1080p' | '2K';
  platform: string;             // 平台选择
  frequency: 'one-time' | 'daily' | 'weekly' | 'monthly';
  includeAudio: boolean;
}
```

输出区域：
```typescript
interface CalculatorOutput {
  totalCredits: number;         // 所需总信用点
  estimatedCost: number;        // 预估费用（USD）
  recommendedPlan: {
    platform: string;
    planName: string;
    monthlyPrice: number;
    savings: string;            // "Save 30% vs pay-per-use"
  };
  competitorComparison: {
    competitor: string;         // "Sora 2"
    estimatedCost: number;
    difference: string;         // "3x more expensive"
  }[];
}
```

UI 设计：
- 左侧：输入表单（slider + dropdown 组合）
- 右侧：实时计算结果
  - 大字体显示总费用
  - 推荐套餐卡片
  - 竞品费用对比柱状图（简单 CSS 实现，无需图表库）
- 底部：详细费用明细表

### 3.8 Model Comparison Tool（模型对比工具 `/tools/compare`）

**页面目标：** 提供 Seedance 与竞品的 side-by-side 详细对比工具。

**目标关键词：**
- `seedance vs sora`、`seedance vs veo 3`（10/月）、`seedance vs kling`

**Schema Markup：** `WebApplication`

**工具功能设计 (`ModelCompare.tsx`)：**

- 顶部：模型选择器（最多选择 3 个模型进行对比）
- 可选模型：Seedance 2.0, Seedance 1.5 Pro, Seedance 1.0 Pro, Sora 2, Veo 3.1, Kling 2.0, Runway Gen-3, Hailuo AI, Minimax
- 对比维度：
  - 基础参数（分辨率、时长、速度）
  - 功能特性（音频、Reference、多镜头）
  - 定价（免费额度、月费、API 价格）
  - 可用性（平台、API、开源状态）
  - 质量评分（主观评分，基于社区反馈）

数据结构：
```typescript
interface AIVideoModel {
  id: string;
  name: string;
  company: string;
  maxResolution: string;
  maxDuration: string;
  hasAudio: boolean;
  hasLipSync: boolean;
  characterConsistency: string;  // "95%+" 
  multiReference: boolean;
  maxReferences: number;
  freeTier: string;
  entryPrice: string;
  apiAvailable: boolean;
  generationTime: string;        // "~60s for 5s clip"
  platforms: string[];
  strengths: string[];
  weaknesses: string[];
}
```


### 3.9 API Integration Guide（API 集成指南 `/api`）

**页面目标：** 为开发者提供完整的 Seedance API 集成文档，覆盖所有可用平台。

**目标关键词：**
- 主关键词：`seedance api`（170/月，CPC $2.58）、`seedance api key`
- 长尾关键词：`seedance 1.0 api`、`seedance pro api`、`fal ai seedance`（10/月）、`replicate seedance`（10/月）、`bytedance seedance api`、`seedance api pricing`、`n8n seedance`、`seedance comfyui`

**Schema Markup：** `TechArticle` + `HowTo`

**页面内容结构：**

1. **平台 Tab 切换 (`PlatformTabs.tsx`)**
   - Tab 1: fal.ai（推荐，最易上手）
   - Tab 2: Replicate
   - Tab 3: Volcano Engine（官方 API）
   - Tab 4: Workflow Integration（n8n / ComfyUI）

2. **每个平台 Tab 内容：**

   **fal.ai Tab：**
   - 快速开始（3 步：安装 → 配置 API Key → 调用）
   - 代码示例（JS/Python/cURL），使用 `CodeBlock.tsx` 组件
     - 语法高亮（Shiki 或 Prism）
     - 语言切换 Tab
     - 一键复制按钮
   - 可用端点列表：
     - `fal-ai/seedance/text-to-video`
     - `fal-ai/seedance/image-to-video`
     - `fal-ai/seedance-lite`
   - 参数说明表 (`ParamTable.tsx`)：
     | 参数 | 类型 | 必填 | 默认值 | 说明 |
     |------|------|------|-------|------|
     | prompt | string | Yes | - | 视频描述文本 |
     | duration | number | No | 5 | 视频时长（秒） |
     | aspect_ratio | string | No | "16:9" | 宽高比 |
     | image_url | string | No | - | 参考图片 URL（i2v） |
   - 定价说明
   - 常见错误排查 (`ErrorGuide.tsx`)

   **Replicate Tab：**
   - 类似结构，使用 Replicate 的 API 格式
   - 可用模型列表
   - Python / JS / cURL 代码示例

   **Volcano Engine Tab：**
   - 注册和认证流程
   - API 规格说明（输入限制、输出格式）
   - REST API 调用示例
   - 注意事项（可能需要企业认证）

   **Workflow Integration Tab：**
   - n8n 集成教程（HTTP Request 节点配置）
   - ComfyUI 节点安装和使用
   - 自动化工作流示例

3. **代码示例数据结构：**
   ```typescript
   interface CodeExample {
     language: 'javascript' | 'python' | 'curl' | 'bash';
     platform: string;
     endpoint: string;
     code: string;
     description: string;
   }
   ```

**CTA 位置：**
- 每个平台 Tab 顶部（"Get API Key →" 链接到对应平台）
- 页面底部（"Need help? Check our FAQ →"）

### 3.10 Registration Guide（注册指南 `/register`）

**页面目标：** 核心差异化页面。解决国际用户注册中国平台的最大痛点，提供详细的 step-by-step 教程。

**目标关键词：**
- 主关键词：`seedance sign up`、`seedance login`
- 长尾关键词：`how to use seedance outside china`、`seedance chinese phone number`、`jimeng registration`、`dreamina sign up`

**Schema Markup：** `HowTo`（详细步骤）

**页面内容结构：**

1. **页面标题区域**
   - H1: "How to Register for Seedance (Chinese Phone Number Guide)"
   - 副标题: "Step-by-step guide to accessing Seedance 2.0 from outside China"
   - 难度指示器：Moderate | 预计时间：10-15 minutes

2. **方法选择区域**
   - 3 个方法卡片，用户选择后展开对应教程：
     - Method A: Virtual Chinese Phone Number（推荐，快速）
     - Method B: International Platforms（无需中国号码）
     - Method C: Pre-registered Account（风险提示）

3. **Method A 详细教程 (`StepByStep.tsx`)**
   
   Step-by-step 图文教程：
   ```typescript
   interface TutorialStep {
     stepNumber: number;
     title: string;
     description: string;
     image?: string;           // 截图路径
     tips?: string[];          // 注意事项
     warningText?: string;     // 警告信息
   }
   ```

   步骤：
   - Step 1: 选择虚拟号码服务（SMS-Activate / 5SIM / MobileSMS.io）
     - 每个服务的优缺点对比表
     - **Affiliate 链接预留位**：`<a href="{AFFILIATE_LINK}" rel="sponsored">`
   - Step 2: 购买中国虚拟号码（配截图）
   - Step 3: 打开即梦/豆包注册页面（配截图）
   - Step 4: 输入虚拟号码（配截图）
   - Step 5: 获取验证码（配截图）
   - Step 6: 完成注册（配截图）
   - Step 7: 绑定邮箱作为备用登录方式（重要提示）

4. **各平台注册流程对比 (`PlatformComparison.tsx`)**
   
   | 平台 | 注册方式 | 需要中国号码 | 难度 | Seedance 版本 |
   |------|---------|------------|------|-------------|
   | Jimeng (China) | 手机号 | Yes | Medium | 1.0/1.5/2.0 |
   | Dreamina (International) | Google/Email | No | Easy | 1.0/1.5 |
   | Xiaoyunque | 手机号 | Yes | Medium | 2.0 |
   | Doubao | 手机号 + 审批 | Yes | Hard | 2.0 |
   | fal.ai | Email/GitHub | No | Easy | 1.0 |
   | Replicate | GitHub | No | Easy | 1.0 |

5. **常见问题和故障排除**
   - "虚拟号码收不到验证码怎么办？"
   - "注册后账号被封怎么办？"
   - "如何切换到国际版？"
   - 每个问题配详细解答

6. **视频教程嵌入位**
   - YouTube 视频嵌入（lazy load）
   - 使用 `<iframe>` + `loading="lazy"` + `srcdoc` 技术优化加载性能

**CTA 位置：**
- 虚拟号码服务推荐处（Affiliate 链接）
- 每个平台注册完成后（"Now start creating →" 链接到 `/guide`）
- 页面底部（"Prefer no registration? Try API →" 链接到 `/api`）

**变现点：**
- 虚拟号码服务 Affiliate 链接（预留位，后续接入）
- 注册代办服务推荐（Phase 2）

### 3.11 Blog（博客 `/blog`）

**页面目标：** 持续产出 SEO 内容，覆盖长尾关键词，建立领域权威。

**目标关键词：** 通过每篇文章覆盖特定长尾关键词

**Schema Markup：** `Blog` + 每篇文章 `BlogPosting`

**博客列表页 (`/blog`)：**

布局：
- 顶部：页面标题 + 搜索框
- 标签筛选栏（横向滚动）：All / Comparison / Tutorial / Prompts / News / Tips
- 文章卡片网格（2列 desktop，1列 mobile）
- 分页（每页 12 篇）

**文章卡片 (`PostCard.tsx`)：**
```typescript
interface BlogPost {
  slug: string;                 // "seedance-vs-sora"
  title: string;                // "Seedance 2.0 vs Sora 2: Which AI Video Generator Wins?"
  description: string;          // SEO description
  date: string;                 // "2026-02-15"
  updatedDate?: string;
  author: string;
  tags: string[];               // ["comparison", "seedance-2.0", "sora"]
  category: string;             // "Comparison"
  readingTime: number;          // 8 (minutes)
  image: string;                // OG image path
  featured: boolean;
}
```

**博客文章页 (`/blog/[slug]`)：**

MDX Frontmatter 格式：
```yaml
---
title: "Seedance 2.0 vs Sora 2: Which AI Video Generator Wins?"
description: "Detailed comparison of Seedance 2.0 and Sora 2 across quality, speed, pricing, and features."
date: "2026-02-15"
author: "Seedance.free Team"
tags: ["comparison", "seedance-2.0", "sora-2"]
category: "Comparison"
image: "/images/og/seedance-vs-sora.png"
---
```

文章页布局：
- 与 Guide 页面类似的三栏布局
- 左侧 TOC（文章内标题）
- 右侧 Sidebar：相关文章 + Ad Slot + Newsletter
- 文章底部：标签列表 + 相关文章推荐（3篇）+ 评论区预留位（Phase 3）

**首批文章计划（5篇）：**

| # | 标题 | 目标关键词 | 预计字数 | 优先级 |
|---|------|----------|---------|-------|
| 1 | Seedance 2.0 vs Sora 2: Which AI Video Generator Wins? | seedance vs sora | 2,500 | P0 |
| 2 | Seedance 2.0 vs Veo 3: Head-to-Head Comparison | seedance vs veo 3 (10/月) | 2,500 | P0 |
| 3 | 10 Best Seedance Prompts for E-commerce Videos | seedance prompt examples | 2,000 | P1 |
| 4 | How to Access Seedance 2.0 Outside China | how to use seedance for free (CPC $2.06) | 2,000 | P1 |
| 5 | Seedance API Tutorial: Generate Videos with Code | seedance api (170/月, CPC $2.58) | 2,500 | P1 |

**相关文章推荐算法：**
- 基于标签匹配度排序
- 同分类文章优先
- 最新文章优先
- 实现：构建时计算每篇文章的相关文章列表，存入 frontmatter 或单独的 JSON

### 3.12 FAQ（常见问题 `/faq`）

**页面目标：** 覆盖高频搜索问题，通过 FAQ Schema 在 Google 搜索结果中直接展示答案。

**目标关键词：**
- `is seedance free`（10/月，Google Trends Breakout）
- `is seedance open source`（20/月）
- `does seedance have audio`（10/月）
- `what is seedance`（10/月，CPC $1.43）
- `how to use seedance`（30/月）
- `is seedance censored`（10/月）
- `do seedance videos have audio`

**Schema Markup：** `FAQPage`（Google 搜索结果直接展示）

```json
{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {
      "@type": "Question",
      "name": "Is Seedance free?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Yes, partially. Jimeng offers ~260 free credits for new users. Xiaoyunque gives 1,200 bonus credits + 120 daily. Doubao offers 10 free generations per day."
      }
    }
  ]
}
```

**FAQ 列表（完整）：**

| 问题 | 简短回答 | 链接到详情页 |
|------|---------|------------|
| What is Seedance? | AI video generation model by ByteDance's Seed team | `/guide` |
| Is Seedance free? | Yes, partially. Multiple free tiers available. | `/pricing` |
| Is Seedance open source? | No, proprietary. But available via API. | `/api` |
| Does Seedance have audio? | 1.5 Pro and 2.0 have native audio. 1.0 does not. | `/versions` |
| Do Seedance videos have audio? | Same as above, with more detail | `/versions` |
| How to use Seedance? | 5 access methods: Jimeng, Dreamina, API, etc. | `/guide` |
| Is Seedance censored? | Content moderation exists on Chinese platforms. | `/guide` |
| What is the maximum video length? | 4-15 seconds per generation, extendable. | `/versions` |
| Do I need a Chinese phone number? | For Jimeng/Doubao/Xiaoyunque yes. Alternatives exist. | `/register` |
| Can I use Seedance for commercial projects? | Yes, paid tier outputs are commercial-licensed. | `/pricing` |
| What's the difference between Seedance versions? | 1.0 (basic) → 1.5 (audio) → 2.0 (full multimodal) | `/versions` |
| How does Seedance compare to Sora? | Seedance: better consistency, cheaper. Sora: longer videos. | `/blog/seedance-vs-sora` |
| Is there a Seedance API? | Yes, via fal.ai, Replicate, and Volcano Engine. | `/api` |
| What is the @Reference system? | Seedance 2.0 feature for multi-file input control. | `/guide` |

**FAQ 组件 (`FAQAccordion.tsx`)：**
- 手风琴展开/折叠交互
- 每个答案末尾有 "Learn more →" 链接到对应详情页
- 支持键盘导航（Accessibility）
- 使用 `<details>` / `<summary>` HTML 元素作为基础，增强 JS 交互
- 分组显示：General / Pricing / Technical / Access

### 3.13 About（关于 `/about`）

**页面目标：** 建立网站可信度，说明网站定位和免责声明。

**内容：**
- 网站介绍：独立维护的 Seedance 资源站，非字节跳动官方
- 免责声明：与 ByteDance 无关联
- 联系方式：Email
- 贡献指南：如何提交内容/修正
- 隐私政策链接
- 使用条款链接


---

## 4. 全局组件

### 4.1 Header（全局导航栏 `Header.tsx`）

**桌面端布局：**
```
┌──────────────────────────────────────────────────────────────┐
│  [Logo] Seedance.free    Guide  Tools▾  API  Blog  Pricing  │
│                                          [Search] [CTA btn] │
└──────────────────────────────────────────────────────────────┘
```

- Logo：文字 Logo "Seedance.free"，链接到首页
- 主导航项：
  - Guide（链接到 `/guide`）
  - Tools（下拉菜单）
    - Prompt Builder（`/tools/prompt-builder`）
    - Model Compare（`/tools/compare`）
    - Credit Calculator（`/tools/calculator`）
    - Prompt Library（`/prompts`）
  - API（链接到 `/api`）
  - Blog（链接到 `/blog`）
  - Pricing（链接到 `/pricing`）
- 右侧：
  - Search 图标（点击展开搜索框）
  - Theme Toggle（暗色/亮色模式切换）
  - CTA 按钮："Get Started"（链接到 `/guide`）

**移动端布局：**
- 汉堡菜单（右上角）
- 点击展开全屏导航菜单
- Logo 居左
- Search 图标 + 汉堡菜单图标居右

**技术实现：**
- `sticky top-0 z-50`，滚动时保持顶部固定
- 背景模糊效果：`backdrop-blur-md bg-white/80 dark:bg-slate-900/80`
- 滚动超过 50px 后添加底部边框阴影
- 使用 `usePathname()` 高亮当前页面对应的导航项

### 4.2 Footer（全局页脚 `Footer.tsx`）

**布局：**
```
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│  Seedance.free          Resources        Tools        Legal  │
│  The complete           Guide            Prompt       Privacy│
│  Seedance resource      Versions         Builder      Terms  │
│  for creators &         Pricing          Calculator   About  │
│  developers.            API Guide        Compare             │
│                         Register                             │
│                         Blog                                 │
│                         FAQ                                  │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  Newsletter: "Stay updated on Seedance"                │  │
│  │  [Email input] [Subscribe]                             │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  ─────────────────────────────────────────────────────────── │
│  © 2026 Seedance.free. Not affiliated with ByteDance.        │
│  [Twitter] [GitHub] [YouTube]                                │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

- 4 列布局（desktop），2 列（tablet），1 列（mobile）
- Newsletter 订阅表单：
  - Phase 1：收集邮箱，存储到 Vercel KV 或 Google Sheets
  - Phase 3：集成 Mailchimp / ConvertKit
- 社交链接：Twitter/X、GitHub、YouTube
- 免责声明：明确标注非 ByteDance 官方

### 4.3 Sidebar（侧边栏 `Sidebar.tsx`）

用于 Guide 页面和 Blog 文章页面。

**内容：**
1. Table of Contents（当前页面目录）
2. 相关文章推荐（3篇）
3. Ad Slot（AdSense 广告位）
4. Newsletter 订阅（简化版）

**技术实现：**
- `sticky top-24`（固定在 Header 下方）
- 桌面端显示，平板和移动端隐藏
- TOC 使用 Intersection Observer 追踪当前阅读位置

### 4.4 Breadcrumbs（面包屑导航 `Breadcrumbs.tsx`）

**全站面包屑，SEO 必备。**

Schema Markup：
```json
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    {
      "@type": "ListItem",
      "position": 1,
      "name": "Home",
      "item": "https://seedance.free"
    },
    {
      "@type": "ListItem",
      "position": 2,
      "name": "Blog",
      "item": "https://seedance.free/blog"
    },
    {
      "@type": "ListItem",
      "position": 3,
      "name": "Seedance vs Sora",
      "item": "https://seedance.free/blog/seedance-vs-sora"
    }
  ]
}
```

- 自动根据 URL 路径生成
- 使用 `navigation.ts` 中的路由映射获取页面标题
- 最后一级不可点击（当前页面）

### 4.5 Search（全站搜索）

**Phase 1：Pagefind（静态搜索）**
- 构建时生成搜索索引
- 零运行时成本
- 支持模糊搜索
- 集成方式：`next build` 后运行 Pagefind 索引构建

**Phase 3：Algolia DocSearch（升级）**
- 更好的搜索体验
- 支持 faceted search
- 搜索分析数据

**搜索 UI (`SearchBar.tsx`)：**
- 快捷键 `Cmd+K` / `Ctrl+K` 打开搜索对话框
- 全屏模态搜索框
- 实时搜索结果预览
- 搜索结果分组（Guide / Blog / Tools / FAQ）
- 键盘导航支持

### 4.6 Dark Mode（暗色模式）

**实现方式：**
- 使用 `next-themes` 库
- 支持三种模式：Light / Dark / System
- 颜色方案：
  - Light：白色背景 + 深色文字
  - Dark：`slate-900` 背景 + 浅色文字
- Tailwind CSS v4 的 `dark:` 变体
- 用户偏好存储在 `localStorage`
- 首次访问跟随系统设置

### 4.7 Mobile Responsiveness（移动端适配）

**断点设计（Tailwind 默认）：**
- `sm`: 640px
- `md`: 768px
- `lg`: 1024px
- `xl`: 1280px

**移动端优先设计原则：**
- 所有表格支持横向滚动
- 导航折叠为汉堡菜单
- Sidebar 隐藏，TOC 变为顶部下拉
- 卡片网格从多列变为单列
- 代码块支持横向滚动
- 触摸友好的按钮尺寸（最小 44x44px）
- 底部固定 CTA 栏（移动端，可关闭）

---

## 5. 技术要求

### 5.1 性能目标

| 指标 | 目标值 | 测量工具 |
|------|-------|---------|
| Lighthouse Performance | 90+ | Chrome DevTools |
| Lighthouse Accessibility | 95+ | Chrome DevTools |
| Lighthouse Best Practices | 95+ | Chrome DevTools |
| Lighthouse SEO | 100 | Chrome DevTools |
| LCP (Largest Contentful Paint) | < 2.5s | Core Web Vitals |
| FID (First Input Delay) | < 100ms | Core Web Vitals |
| CLS (Cumulative Layout Shift) | < 0.1 | Core Web Vitals |
| TTFB (Time to First Byte) | < 200ms | WebPageTest |
| Total Page Size | < 500KB (initial) | Chrome DevTools |

### 5.2 图片优化

- 使用 `next/image` 组件自动优化
- 格式：自动选择 WebP/AVIF（浏览器支持检测）
- 响应式图片：`sizes` 属性根据断点设置
- Lazy loading：首屏以下图片延迟加载
- Placeholder：使用 `blur` placeholder（构建时生成 base64 缩略图）
- OG 图片：每个页面生成专属 Open Graph 图片（1200x630px）
  - 使用 `@vercel/og` 或 Satori 动态生成
  - 包含页面标题 + 网站 Logo + 品牌色背景

### 5.3 字体策略

```css
/* 系统字体栈（正文） */
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 
             'Helvetica Neue', Arial, sans-serif;

/* Inter（仅标题） */
font-family: 'Inter', var(--font-system);
```

- 正文使用系统字体栈（零加载时间）
- 标题使用 Inter（通过 `next/font/google` 自动优化）
- `font-display: swap` 避免 FOIT
- 仅加载 Inter 的 600/700/800 字重

### 5.4 Analytics & Tracking

**Google Analytics 4：**
- 使用 `@next/third-parties` 的 `GoogleAnalytics` 组件
- 事件追踪：
  - `copy_prompt`：用户复制 Prompt
  - `use_calculator`：使用费用计算器
  - `use_prompt_builder`：使用 Prompt 构建器
  - `click_cta`：点击 CTA 按钮（标注位置和目标）
  - `click_affiliate`：点击 Affiliate 链接
  - `search`：使用站内搜索
  - `newsletter_signup`：订阅 Newsletter

**Google Search Console：**
- 提交 sitemap.xml
- 监控索引状态
- 追踪关键词排名

### 5.5 AdSense 广告位规划

**广告位类型和位置：**

| 广告位 ID | 位置 | 类型 | 页面 |
|----------|------|------|------|
| `ad-sidebar` | 右侧边栏 | Display (300x250) | Guide, Blog |
| `ad-article-mid-1` | 文章中间（Section 3后） | In-article | Guide, Blog |
| `ad-article-mid-2` | 文章中间（Section 5后） | In-article | Guide, Blog |
| `ad-article-bottom` | 文章底部 | Display (728x90) | Guide, Blog |
| `ad-tool-bottom` | 工具页底部 | Display (728x90) | Tools |
| `ad-homepage-mid` | 首页中间 | Display (728x90) | Homepage |

**AdSlot 组件 (`AdSlot.tsx`)：**
```typescript
interface AdSlotProps {
  slotId: string;
  format: 'auto' | 'rectangle' | 'horizontal' | 'vertical';
  className?: string;
}
```

- 使用 `<ins class="adsbygoogle">` 标签
- Lazy load：仅在广告位进入视口时加载
- 占位符：广告加载前显示灰色占位区域（避免 CLS）
- 暗色模式适配：广告容器背景色适配

**AdSense 脚本加载 (`AdSenseScript.tsx`)：**
- 使用 `next/script` 的 `afterInteractive` 策略
- 仅在生产环境加载
- 环境变量：`NEXT_PUBLIC_ADSENSE_ID`

### 5.6 SEO 技术实现

**Meta Tags (`MetaTags.tsx`)：**
- 每个页面独立的 `title` 和 `description`
- Open Graph tags（og:title, og:description, og:image, og:url）
- Twitter Card tags（twitter:card, twitter:title, twitter:description, twitter:image）
- Canonical URL
- 使用 Next.js 15 的 `metadata` API（App Router）

**Sitemap 自动生成：**
```typescript
// src/app/sitemap.ts
export default async function sitemap(): Promise<MetadataRoute.Sitemap> {
  const blogPosts = await getAllBlogPosts();
  
  const staticPages = [
    { url: 'https://seedance.free', changeFrequency: 'weekly', priority: 1.0 },
    { url: 'https://seedance.free/guide', changeFrequency: 'weekly', priority: 0.9 },
    { url: 'https://seedance.free/versions', changeFrequency: 'monthly', priority: 0.8 },
    { url: 'https://seedance.free/pricing', changeFrequency: 'weekly', priority: 0.8 },
    { url: 'https://seedance.free/prompts', changeFrequency: 'weekly', priority: 0.8 },
    { url: 'https://seedance.free/api', changeFrequency: 'monthly', priority: 0.7 },
    { url: 'https://seedance.free/register', changeFrequency: 'monthly', priority: 0.7 },
    { url: 'https://seedance.free/tools/prompt-builder', changeFrequency: 'monthly', priority: 0.6 },
    { url: 'https://seedance.free/tools/calculator', changeFrequency: 'monthly', priority: 0.6 },
    { url: 'https://seedance.free/tools/compare', changeFrequency: 'monthly', priority: 0.6 },
    { url: 'https://seedance.free/blog', changeFrequency: 'daily', priority: 0.7 },
    { url: 'https://seedance.free/faq', changeFrequency: 'monthly', priority: 0.7 },
  ];

  const blogPages = blogPosts.map(post => ({
    url: `https://seedance.free/blog/${post.slug}`,
    lastModified: post.updatedDate || post.date,
    changeFrequency: 'monthly' as const,
    priority: 0.6,
  }));

  return [...staticPages, ...blogPages];
}
```

**robots.txt：**
```
User-agent: *
Allow: /
Disallow: /api/  # 如果有内部 API 路由

Sitemap: https://seedance.free/sitemap.xml
```

**结构化数据汇总：**

| 页面 | Schema 类型 | 用途 |
|------|-----------|------|
| Homepage | WebSite + Organization + SearchAction | 站点信息 + 搜索框 |
| Guide | HowTo + Article | 教程步骤 + 文章 |
| Versions | Product (每个版本) | 产品信息 |
| Pricing | Product + Offer | 产品定价 |
| Prompts | ItemList + CreativeWork | 内容列表 |
| Tools | WebApplication | 工具应用 |
| API | TechArticle + HowTo | 技术文档 |
| Register | HowTo | 教程步骤 |
| Blog | Blog + BlogPosting | 博客文章 |
| FAQ | FAQPage | FAQ 搜索结果展示 |
| All Pages | BreadcrumbList | 面包屑导航 |


---

## 6. SEO 策略

### 6.1 关键词矩阵（基于实际搜索数据）

**Tier 1 - 核心关键词（高搜索量）：**

| 关键词 | 月搜索量 | KD | CPC | 目标页面 | 优先级 |
|--------|---------|-----|-----|---------|-------|
| seedance | 4,400 | 39 | $1.33 | Homepage | P0 |
| seedance 1.0 | 2,400 | 30 | $1.26 | /versions | P0 |
| seedance ai | 1,300 | 23 | $1.15 | Homepage | P0 |
| bytedance/seedance-v1-pro-i2v-480p | 1,600 | 11 | $0.00 | /api | P0 |
| seedance 1.0 free | 480 | 14 | $0.69 | /pricing | P0 |
| seedance 1.0 pro | 480 | 21 | $1.60 | /versions | P0 |

**Tier 2 - 商业意图关键词（高 CPC）：**

| 关键词 | 月搜索量 | KD | CPC | 目标页面 | 优先级 |
|--------|---------|-----|-----|---------|-------|
| seedance api | 170 | 9 | $2.58 | /api | P0 |
| seedance price | 110 | 18 | $1.93 | /pricing | P1 |
| seedance video generator | 70 | 14 | $2.56 | Homepage | P1 |
| seedance text to video | 30 | - | $2.84 | /guide | P1 |
| seedance by bytedance | 10 | - | $2.35 | Homepage | P2 |
| seedance free trial | 10 | - | $2.22 | /pricing | P2 |

**Tier 3 - 长尾关键词（低竞争，Breakout 趋势）：**

| 关键词 | 月搜索量 | 趋势 | 目标页面 |
|--------|---------|------|---------|
| seedance 2.0 | 30 | Breakout (Google Trends #1) | /versions |
| how to use seedance | 30 | Breakout | /guide |
| is seedance free | 10 | Breakout | /faq + /pricing |
| what is seedance | 10 | Breakout | /faq + Homepage |
| seedance vs veo 3 | 10 | Breakout | /blog/seedance-vs-veo3 |
| is seedance open source | 20 | - | /faq |
| does seedance have audio | 10 | - | /faq + /versions |
| is seedance censored | 10 | - | /faq |
| seedance comfyui | 10 | - | /api |
| seedance n8n | 10 | - | /api |

### 6.2 On-Page SEO 规范

**Title Tag 格式：**
- Homepage: `Seedance.free - The Complete Seedance AI Video Resource`
- Guide: `How to Use Seedance: The Complete Guide (2026) | Seedance.free`
- Versions: `Seedance Versions Compared: 1.0 vs 1.5 vs 2.0 | Seedance.free`
- Pricing: `Seedance Pricing & Free Methods (2026) | Seedance.free`
- Blog: `{Article Title} | Seedance.free`
- 规则：主关键词在前，品牌名在后，总长度 50-60 字符

**Meta Description 格式：**
- 包含主关键词
- 包含 CTA 或价值主张
- 长度 150-160 字符
- 示例：`Learn how to use Seedance 2.0 for AI video generation. Free access methods, prompt tips, API guide, and version comparison. Updated Feb 2026.`

**H 标签层级：**
- 每页仅一个 `<h1>`
- `<h2>` 用于主要章节
- `<h3>` 用于子章节
- 关键词自然融入标题

**内部链接密度：**
- 每 500 字至少 2-3 个内部链接
- 锚文本使用目标关键词（避免 "click here"）
- 每个页面至少链接到 3 个其他页面

**图片 SEO：**
- 所有图片使用描述性 `alt` 文本
- 文件名使用关键词（`seedance-2.0-version-comparison.webp`）
- 使用 `next/image` 自动生成 `srcset`

### 6.3 Technical SEO

- **HTTPS**：Vercel 自动提供 SSL
- **Canonical URLs**：每个页面设置 canonical，避免重复内容
- **Hreflang**：Phase 4 多语言时添加
- **XML Sitemap**：自动生成，提交到 Search Console
- **robots.txt**：允许所有页面被索引
- **页面速度**：SSG + CDN 确保快速加载
- **Mobile-first**：响应式设计，Google Mobile-First Indexing 友好
- **Core Web Vitals**：持续监控和优化

### 6.4 内容更新策略

| 频率 | 内容类型 | 说明 |
|------|---------|------|
| 每周 | Blog 文章 | 1-2 篇新文章，覆盖新的长尾关键词 |
| 每两周 | Prompt Library | 新增 5-10 个 prompt |
| 每月 | Guide / Versions / Pricing | 更新版本信息和价格变动 |
| 即时 | FAQ | 根据搜索数据新增高频问题 |
| 季度 | 全站 SEO 审计 | 检查排名、修复技术问题 |

---

## 7. 变现策略

### 7.1 Phase 1-2：AdSense 广告

**预期收入模型：**
- 假设 RPM（每千次展示收入）= $5-15（科技/AI 领域）
- 10,000 月 PV → $50-150/月
- 50,000 月 PV → $250-750/月
- 200,000 月 PV → $1,000-3,000/月

**广告位优化：**
- 文章内广告（最高 RPM）
- Sidebar 广告（持续曝光）
- 工具页底部广告
- 避免过度广告影响用户体验（每页最多 3 个广告位）

### 7.2 Phase 2：Affiliate 收入

**潜在 Affiliate 合作：**
- 虚拟号码服务（SMS-Activate, 5SIM）→ 注册指南页面
- API 平台（fal.ai, Replicate）→ API 指南页面
- VPN 服务 → 海外访问相关内容
- AI 工具推荐 → 博客文章

**实现方式：**
- 使用 `rel="sponsored"` 标记 Affiliate 链接
- 在页面底部添加 Affiliate 声明
- 追踪 Affiliate 点击（GA4 事件）

### 7.3 Phase 3-4：付费内容/会员系统

**付费内容类型：**
1. **Premium Prompts**：高级 Prompt 模板（$5-10 一次性购买或会员解锁）
2. **Video Tutorials**：视频教程系列（$19-49 课程）
3. **Prompt Packs**：按行业分类的 Prompt 包（$9-29）
4. **Newsletter Premium**：付费 Newsletter（$5/月）

**会员系统设计（Phase 4）：**
```typescript
interface MembershipTier {
  name: 'free' | 'pro' | 'team';
  price: number;              // 月费 USD
  features: {
    promptAccess: 'basic' | 'all';
    toolsAccess: 'basic' | 'all';
    adsRemoved: boolean;
    prioritySupport: boolean;
    apiCredits: number;
  };
}

const tiers: MembershipTier[] = [
  {
    name: 'free',
    price: 0,
    features: {
      promptAccess: 'basic',
      toolsAccess: 'basic',
      adsRemoved: false,
      prioritySupport: false,
      apiCredits: 0,
    }
  },
  {
    name: 'pro',
    price: 9,
    features: {
      promptAccess: 'all',
      toolsAccess: 'all',
      adsRemoved: true,
      prioritySupport: true,
      apiCredits: 100,
    }
  }
];
```

**技术实现（Phase 4）：**
- 认证：NextAuth.js（Google / GitHub / Email 登录）
- 支付：Stripe（订阅 + 一次性购买）
- 数据库：Supabase（用户数据 + 订阅状态）
- 内容保护：Server Component 中检查用户权限

---

## 8. 分期交付计划

### Phase 1 — MVP（3天内上线）

**目标：** 最小可用产品上线，开始被 Google 索引。

**交付内容：**

| 任务 | 优先级 | 预计工时 | 说明 |
|------|-------|---------|------|
| 项目初始化 | P0 | 2h | Next.js 15 + Tailwind v4 + TypeScript + pnpm |
| 全局布局 | P0 | 3h | Header + Footer + Breadcrumbs + Dark Mode |
| Homepage | P0 | 4h | Hero + QuickNavCards + ComparisonSummary |
| Complete Guide | P0 | 4h | 基于现有 MD 内容，添加 TOC + CTA |
| FAQ | P0 | 2h | FAQAccordion + FAQ Schema |
| SEO 基础 | P0 | 2h | Meta tags + Sitemap + robots.txt + JsonLd |
| GA4 + Search Console | P0 | 1h | 集成分析工具 |
| Vercel 部署 | P0 | 1h | 域名配置 + 自动部署 |
| **总计** | | **~19h** | |

**验收标准：**
- Lighthouse Performance 90+
- 所有页面可正常访问
- Google Search Console 已提交 sitemap
- GA4 数据正常采集
- 暗色模式正常工作
- 移动端响应式正常

### Phase 2 — 核心功能（1周内）

**目标：** 完善核心内容页面和工具，覆盖主要关键词。

**交付内容：**

| 任务 | 优先级 | 预计工时 | 说明 |
|------|-------|---------|------|
| Prompt Builder Tool | P0 | 6h | 交互式表单 + 实时预览 + @Reference 辅助 |
| Version Comparison | P0 | 4h | 交互式对比表 + 版本卡片 + 决策流程图 |
| Pricing Page | P0 | 4h | 价格对比表 + Free Methods 专区 + 内嵌计算器 |
| Registration Guide | P0 | 4h | Step-by-step 教程 + 平台对比 + 故障排除 |
| Blog 系统 | P0 | 4h | MDX 集成 + 列表页 + 文章页 + 标签系统 |
| Blog 文章 x2 | P1 | 6h | Seedance vs Sora + Seedance vs Veo 3 |
| Prompt Library | P1 | 4h | 分类浏览 + 搜索筛选 + 复制功能 + 初始30个prompt |
| 全站搜索 (Pagefind) | P1 | 2h | 静态搜索索引 + 搜索 UI |
| **总计** | | **~34h** | |

**验收标准：**
- 所有工具页面交互正常
- Prompt Builder 生成的 prompt 格式正确
- 博客文章 SEO 元数据完整
- 搜索功能可用
- 所有内部链接正常

### Phase 3 — 完善与变现（2周内）

**目标：** 补充剩余内容，接入广告变现，建立用户触达渠道。

**交付内容：**

| 任务 | 优先级 | 预计工时 | 说明 |
|------|-------|---------|------|
| Credit Calculator | P1 | 4h | 完整版费用计算器 |
| Model Compare Tool | P1 | 4h | Side-by-side 模型对比 |
| API Guide | P1 | 6h | 多平台 Tab + 代码示例 + 参数表 |
| Blog 文章 x3 | P1 | 9h | E-commerce Prompts + Outside China + API Tutorial |
| AdSense 集成 | P1 | 3h | 广告位组件 + 脚本加载 + 位置优化 |
| Newsletter 系统 | P2 | 3h | 订阅表单 + 邮箱收集 + 欢迎邮件 |
| OG 图片生成 | P2 | 3h | 动态 OG 图片（@vercel/og） |
| 性能优化 | P2 | 3h | Lighthouse 审计 + Core Web Vitals 优化 |
| **总计** | | **~35h** | |

**验收标准：**
- AdSense 广告正常展示
- Newsletter 订阅功能正常
- 所有工具页面功能完整
- Lighthouse 全项 90+
- 5 篇博客文章上线

### Phase 4 — 增长与付费（1个月内）

**目标：** 建立付费内容体系，扩展用户群。

**交付内容：**

| 任务 | 优先级 | 预计工时 | 说明 |
|------|-------|---------|------|
| 用户认证系统 | P2 | 8h | NextAuth.js + Google/GitHub 登录 |
| 付费内容/会员 | P2 | 12h | Stripe 集成 + 内容保护 + 会员页面 |
| 用户提交 Prompt | P2 | 6h | 提交表单 + 审核流程 + Supabase 存储 |
| 多语言支持 | P3 | 8h | next-intl + 中文/日文翻译 |
| Blog 文章 x5 | P2 | 15h | 持续内容产出 |
| Affiliate 链接接入 | P2 | 2h | 虚拟号码服务 + API 平台 |
| 搜索升级 (Algolia) | P3 | 4h | 替换 Pagefind |
| **总计** | | **~55h** | |

**验收标准：**
- 用户可注册登录
- 付费内容购买流程完整
- Stripe 支付正常
- 至少 10 篇博客文章
- Newsletter 订阅用户 500+

---

## 附录

### A. 环境变量清单

```env
# Analytics
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
NEXT_PUBLIC_ADSENSE_ID=ca-pub-XXXXXXXXXX

# Search
NEXT_PUBLIC_ALGOLIA_APP_ID=           # Phase 3
NEXT_PUBLIC_ALGOLIA_SEARCH_KEY=       # Phase 3

# Newsletter
NEWSLETTER_API_KEY=                    # Mailchimp/ConvertKit

# Auth (Phase 4)
NEXTAUTH_SECRET=
NEXTAUTH_URL=https://seedance.free
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Payment (Phase 4)
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

# Database (Phase 4)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
```

### B. 第三方依赖清单

```json
{
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@next/third-parties": "^15.0.0",
    "next-themes": "^0.4.0",
    "next-mdx-remote": "^5.0.0",
    "gray-matter": "^4.0.3",
    "shiki": "^1.0.0",
    "lucide-react": "^0.400.0",
    "clsx": "^2.1.0",
    "tailwind-merge": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "tailwindcss": "^4.0.0",
    "@tailwindcss/typography": "^0.5.0",
    "eslint": "^9.0.0",
    "eslint-config-next": "^15.0.0",
    "prettier": "^3.0.0",
    "prettier-plugin-tailwindcss": "^0.6.0",
    "pagefind": "^1.0.0"
  }
}
```

### C. 关键数据来源

- **关键词数据**：Semrush broad match export（2026-02-13），美国市场
- **Google Trends 数据**：Top queries + Rising queries（2025-02-13 至 2026-02-13），美国市场
- **产品信息**：基于 `how-to-use-seedance-complete-guide.md` 内容，截至 2026 年 2 月
- **定价数据**：各平台官方公开信息，需定期更新

### D. 风险与缓解

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|---------|
| Seedance 品牌方投诉 | 高 | 低 | 明确标注非官方、添加免责声明、遵守商标使用规范 |
| 关键词竞争加剧 | 中 | 中 | 持续产出高质量内容、建立外链、先发优势 |
| 平台定价/政策变动 | 中 | 高 | ISR 定期更新、建立数据更新流程 |
| AdSense 审核不通过 | 中 | 低 | 确保内容原创性、避免违规内容、备选 Mediavine/Ezoic |
| 虚拟号码服务失效 | 低 | 中 | 定期验证推荐服务、提供多个替代方案 |

---

> 文档结束。本 PRD 应作为开发团队的唯一参考文档，所有页面设计和功能实现以本文档为准。如有疑问或需要变更，请更新本文档并标注版本号。
